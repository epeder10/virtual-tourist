//
//  PhotoSearchResults.swift
//  Virtual Tourist
//
//  Created by Eric Pedersen on 1/20/19.
//  Copyright © 2019 Eric Pedersen. All rights reserved.
//

import Foundation

/*
 { "id": "46762563172", "owner": "51639887@N04", "secret": "3c44b9cd35", "server": "4829", "farm": 5, "title": "", "ispublic": 1, "isfriend": 0, "isfamily": 0 },
 

class Photo: NSObject, Mappable {
    var id: String?
    var owner: String?
    var secret: String?
    var server: String?
    var farm: Int?
    var title: String?
    var isPublic: Int?
    var isFriend: Int?
    var isFamily: Int?
    
    override init() {
        super.init()
    }
    
    convenience required init?(map: Map) {
        self.init()
    }
    
    func mapping(map: Map) {
        self.id     <- map["id"]
        self.owner     <- map["owner"]
        self.secret     <- map["secret"]
        self.server     <- map["server"]
        self.farm     <- map["farm"]
        self.title     <- map["title"]
        self.isPublic     <- map["ispublic"]
        self.isFriend     <- map["isfriend"]
        self.isFamily     <- map["isfamily"]
    }
}

class PhotoResults: NSObject, Mappable {
    var page: Int?
    var pages: Int?
    var perpage: Int?
    var total: Int?
    var photo: [Photo]?
    var stat: String?
    
    override init() {
        super.init()
    }
    
    convenience required init?(map: Map) {
        self.init()
    }
    
    func mapping(map: Map) {
        self.page     <- map["page"]
        self.pages     <- map["pages"]
        self.perpage     <- map["perpage"]
        self.total     <- map["total"]
        self.photo     <- map["photo"]
        self.stat     <- map["stat"]
    }
}

class Photos: NSObject, Mappable {
    var photos: Photo?
    
    override init() {
        super.init()
    }
    
    convenience required init?(map: Map) {
        self.init()
    }
    
    func mapping(map: Map) {
        self.photos     <- map["photos"]
    }
}
 */

